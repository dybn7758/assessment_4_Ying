apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendations-service
  namespace: recommendations
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recommendations-service
  template:
    metadata:
      labels:
        app: recommendations-service
    spec:
      serviceAccountName: recommendations-service-account
      containers:
        - name: recommendations-service
          image: 388691194728.dkr.ecr.us-east-1.amazonaws.com/assessment-4-ying-recommendations:latest

          envFrom:
            - configMapRef:
                name: recommendations-config
            - secretRef:
                name: recommendations-secret

          ports:
            - containerPort: 8000

          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          startupProbe:
            httpGet:
              path: /health
              port: 8000
            failureThreshold: 30
            periodSeconds: 2

          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 20

---
apiVersion: v1
kind: Service
metadata:
  name: recommendations-service
  namespace: recommendations
spec:
  selector:
    app: recommendations-service
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP